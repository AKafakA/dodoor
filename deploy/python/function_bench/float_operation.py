import math
import random


if __name__ == "__main__":
    N = random.randint(1000000, 10000000)
    for i in range(0, N):
        sin_i = math.sin(i)
        cos_i = math.cos(i)
        sqrt_i = math.sqrt(i)
